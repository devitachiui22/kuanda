<!DOCTYPE html>
<html lang="pt-AO">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Admin Premium</title>
    
    <!-- Fontes e Ícones de Alta Fidelidade -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- FontAwesome 6.5.1 -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <!-- Bootstrap 5 Grid System -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* =========================================
           SISTEMA DE DESIGN LUXURY MODULAR
           ========================================= */
        :root {
            --bg-page: #f8fafc;
            --accent: #4f46e5;
            --accent-soft: rgba(79, 70, 229, 0.1);
            --text-dark: #0f172a;
            --text-muted: #64748b;
            --card-bg: #ffffff;
            --radius-lg: 24px;
            --radius-md: 16px;
            --shadow: 0 10px 40px -10px rgba(0,0,0,0.05);
        }

        /* RESET DE SUBINHADOS E LINKS */
        * {
            text-decoration: none !important; /* Elimina sublinhados em tudo, inclusive na navbar */
            outline: none !important;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: var(--text-dark);
            margin: 0;
            padding-bottom: 80px;
            
            /* CONFIGURAÇÃO DA IMAGEM DE FUNDO */
            background-image: 
                linear-gradient(rgba(248, 250, 252, 0.88), rgba(248, 250, 252, 0.88)), /* Camada Light para suavizar */
                url('https://www.callofduty.com/content/dam/atvi/callofduty/cod-touchui/store/games/mw3/overview/Store_GamesPDP_Hero01.png?imwidth=1080'); /* Imagem Ultra HD do GTA VI */
            
            background-attachment: fixed; /* Efeito Parallax: os cards deslizam sobre a imagem */
            background-size: cover;
            background-position: center top;
            background-repeat: no-repeat;
        }

        /* Pequeno ajuste nos cards para eles brilharem mais sobre o fundo com imagem */
        .modular-card {
            backdrop-filter: blur(10px); /* Efeito de vidro borrado no fundo do card */
            background: rgba(255, 255, 255, 0.95); /* Branco levemente transparente */
            border: 1px solid rgba(255, 255, 255, 1);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08); /* Sombra mais profunda para dar destaque */
        }

        /* Espaçamento de 2cm (aprox 75px) para a Navbar */
        .page-container {
            margin-top: 75px; 
            max-width: 950px;
            margin-left: auto;
            margin-right: auto;
            padding: 0 20px;
        }

        /* HEADER INDEPENDENTE */
        .form-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding: 10px 5px;
        }

        .form-header h1 {
            font-weight: 800;
            font-size: 1.8rem;
            letter-spacing: -1px;
            color: var(--text-dark);
            margin: 0;
        }

        .btn-close-custom {
            background: white;
            color: var(--text-muted);
            width: 45px;
            height: 45px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: var(--shadow);
            transition: 0.3s;
        }
        .btn-close-custom:hover {
            color: var(--accent);
            transform: rotate(90deg);
        }

        /* CARDS MODULARES INDIVIDUAIS */
        .modular-card {
            background: var(--card-bg);
            border-radius: var(--radius-lg);
            padding: 30px;
            margin-bottom: 20px;
            box-shadow: var(--shadow);
            border: 1px solid rgba(255,255,255,0.8);
            transition: transform 0.3s ease;
        }

        .card-label-box {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 25px;
        }

        .icon-circle {
            width: 40px;
            height: 40px;
            background: var(--accent-soft);
            color: var(--accent);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
        }

        .card-label-box h4 {
            font-weight: 800;
            font-size: 1.1rem;
            margin: 0;
            color: var(--text-dark);
        }

        /* ESTILIZAÇÃO DE INPUTS FORMULARES */
        .form-label {
            font-weight: 700;
            font-size: 0.8rem;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
        }

        .form-control, .form-select {
            border-radius: 14px;
            padding: 12px 16px;
            border: 2px solid #f1f5f9;
            background: #fcfdfe;
            font-weight: 600;
            font-size: 0.95rem;
            transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .form-control:focus {
            border-color: var(--accent);
            background: white;
            box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.1);
        }

        /* DOWNLOAD LINKS DINÂMICOS */
        .link-item {
            display: grid;
            grid-template-columns: 1fr 2fr 50px;
            gap: 12px;
            margin-bottom: 12px;
            background: #f8fafc;
            padding: 10px;
            border-radius: 16px;
        }

        .btn-del-link {
            background: #fee2e2;
            color: #ef4444;
            border: none;
            border-radius: 12px;
            transition: 0.2s;
        }
        .btn-del-link:hover { background: #ef4444; color: white; }

        .btn-add-more {
            width: 100%;
            padding: 14px;
            background: white;
            border: 2px dashed #e2e8f0;
            border-radius: 16px;
            color: var(--text-muted);
            font-weight: 700;
            font-size: 0.85rem;
            transition: 0.3s;
        }
        .btn-add-more:hover { border-color: var(--accent); color: var(--accent); }

        /* TRAILER BOX */
        .trailer-box {
            background: linear-gradient(135deg, #f5f3ff 0%, #ffffff 100%);
            border: 1px solid #e0e7ff;
            border-radius: var(--radius-md);
            padding: 20px;
        }

        /* BOTÃO DE SALVAR PRINCIPAL */
        .btn-publish {
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);
            color: white;
            border: none;
            border-radius: 20px;
            padding: 20px;
            width: 100%;
            font-weight: 800;
            font-size: 1.1rem;
            box-shadow: 0 10px 25px rgba(79, 70, 229, 0.3);
            transition: 0.4s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            margin-top: 20px;
        }
        .btn-publish:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(79, 70, 229, 0.4);
            filter: brightness(1.1);
        }

        /* Custom Switch */
        .form-switch .form-check-input {
            width: 3em; height: 1.5em; cursor: pointer;
        }
        .form-switch .form-check-input:checked {
            background-color: #10b981;
            border-color: #10b981;
        }

        /* Animação Suave */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .modular-card { animation: fadeInUp 0.5s ease backwards; }
    </style>
</head>
<body>

<div class="page-container">
    
    <!-- HEADER -->
    <header class="form-header">
        <h1><%= title %></h1>
        <a href="/admin/jogos" class="btn-close-custom" title="Fechar">
            <i class="fa-solid fa-xmark"></i>
        </a>
    </header>

    <form action="<%= action %>" method="POST" enctype="multipart/form-data">

        <!-- CARD 1: INFORMAÇÕES BÁSICAS -->
        <div class="modular-card" style="animation-delay: 0.1s;">
            <div class="card-label-box">
                <div class="icon-circle"><i class="fa-solid fa-gamepad"></i></div>
                <h4>Detalhes do Título</h4>
            </div>
            <div class="row g-4">
                <div class="col-md-8">
                    <label class="form-label">Nome Oficial</label>
                    <input type="text" name="titulo" class="form-control" value="<%= jogo ? jogo.titulo : '' %>" required placeholder="Ex: Elden Ring">
                </div>
                <div class="col-md-4">
                    <label class="form-label">Preço (Kz)</label>
                    <input type="number" name="preco" class="form-control" value="<%= jogo ? jogo.preco : '' %>" placeholder="Grátis se vazio">
                </div>
                <div class="col-md-4">
                    <label class="form-label">Plataforma</label>
                    <select name="plataforma" class="form-select">
                        <option value="PC" <%= jogo && jogo.plataforma == 'PC' ? 'selected' : '' %>>Computador (PC)</option>
                        <option value="PSN" <%= jogo && jogo.plataforma == 'PSN' ? 'selected' : '' %>>PlayStation</option>
                        <option value="XBOX" <%= jogo && jogo.plataforma == 'XBOX' ? 'selected' : '' %>>Xbox Series</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Gênero</label>
                    <select name="genero" class="form-select">
                        <option value="Ação" <%= jogo && jogo.genero == 'Ação' ? 'selected' : '' %>>Ação</option>
                        <option value="Aventura" <%= jogo && jogo.genero == 'Aventura' ? 'selected' : '' %>>Aventura</option>
                        <option value="RPG" <%= jogo && jogo.genero == 'RPG' ? 'selected' : '' %>>RPG</option>
                        <option value="Terror" <%= jogo && jogo.genero == 'Terror' ? 'selected' : '' %>>Terror</option>
                        <option value="Terror" <%= jogo && jogo.genero == 'Luta' ? 'selected' : '' %>>Luta</option>
                        <option value="Terror" <%= jogo && jogo.genero == 'Racing' ? 'selected' : '' %>>Racing</option>
                        <option value="Terror" <%= jogo && jogo.genero == 'FPS' ? 'selected' : '' %>>FPS</option>
                        <option value="Terror" <%= jogo && jogo.genero == 'Shotter' ? 'selected' : '' %>Shotter</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label class="form-label">Classificação</label>
                    <select name="classificacao" class="form-select">
                        <option value="L" <%= jogo && jogo.classificacao == 'L' ? 'selected' : '' %>>Livre</option>
                        <option value="16" <%= jogo && jogo.classificacao == '14' ? 'selected' : '' %>>16+</option>
                        <option value="16" <%= jogo && jogo.classificacao == '15' ? 'selected' : '' %>>16+</option>
                        <option value="16" <%= jogo && jogo.classificacao == '16' ? 'selected' : '' %>>16+</option>
                        <option value="16" <%= jogo && jogo.classificacao == '17' ? 'selected' : '' %>>16+</option>
                        <option value="18" <%= jogo && jogo.classificacao == '18' ? 'selected' : '' %>>18+</option>
                    </select>
                </div>
            </div>
        </div>

        <!-- CARD 2: TRAILER -->
        <div class="modular-card" style="animation-delay: 0.2s;">
            <div class="card-label-box">
                <div class="icon-circle"><i class="fa-solid fa-play"></i></div>
                <h4>Trailer Promocional</h4>
            </div>
            <div class="trailer-box">
                <label class="form-label">Link do YouTube / Vimeo</label>
                <div class="input-group">
                    <span class="input-group-text bg-white border-end-0" style="border-radius: 12px 0 0 12px;"><i class="fa-brands fa-youtube text-danger"></i></span>
                    <input type="url" name="trailer_url" class="form-control border-start-0" value="<%= jogo ? jogo.trailer_url : '' %>" placeholder="https://youtube.com/...">
                </div>
            </div>
        </div>

        <!-- CARD 3: LINKS DE DOWNLOAD -->
        <div class="modular-card" style="animation-delay: 0.3s;">
            <div class="card-label-box">
                <div class="icon-circle"><i class="fa-solid fa-link"></i></div>
                <h4>Servidores de Download</h4>
            </div>
            <div id="linksContainer">
                <% if (typeof links !== 'undefined' && links.length > 0) { %>
                    <% links.forEach((link, index) => { %>
                        <div class="link-item">
                            <input type="text" name="links_labels[]" class="form-control" placeholder="Servidor" value="<%= link.label %>" required>
                            <input type="url" name="links_urls[]" class="form-control" placeholder="URL" value="<%= link.url %>" required>
                            <button type="button" class="btn-del-link" onclick="this.parentElement.remove()"><i class="fa-solid fa-trash-can"></i></button>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="link-item">
                        <input type="text" name="links_labels[]" class="form-control" placeholder="Ex: Mega.nz" required>
                        <input type="url" name="links_urls[]" class="form-control" placeholder="https://..." required>
                        <button type="button" class="btn-del-link" onclick="this.parentElement.remove()"><i class="fa-solid fa-trash-can"></i></button>
                    </div>
                <% } %>
            </div>
            <button type="button" class="btn-add-more" onclick="addLink()">
                <i class="fa-solid fa-plus-circle me-2"></i> Adicionar Servidor
            </button>
        </div>

        <!-- CARD 4: MÍDIA -->
        <div class="modular-card" style="animation-delay: 0.4s;">
            <div class="card-label-box">
                <div class="icon-circle"><i class="fa-solid fa-image"></i></div>
                <h4>Identidade Visual</h4>
            </div>
            <div class="row g-4">
                <div class="col-md-6">
                    <label class="form-label">Capa (Retrato)</label>
                    <input type="file" name="capa" class="form-control" accept="image/*" <%= jogo ? '' : 'required' %>>
                </div>
                <div class="col-md-6">
                    <label class="form-label">Banner (Paisagem)</label>
                    <input type="file" name="banner" class="form-control" accept="image/*">
                </div>
                <div class="col-12">
                    <label class="form-label">Capturas de Tela (Multiplas)</label>
                    <input type="file" name="screenshots" class="form-control" accept="image/*" multiple>
                </div>
            </div>
        </div>

        <!-- CARD 5: DESCRIÇÕES E BOTÃO FINAL -->
        <div class="modular-card" style="animation-delay: 0.5s;">
            <div class="card-label-box">
                <div class="icon-circle"><i class="fa-solid fa-align-left"></i></div>
                <h4>Textos e Publicação</h4>
            </div>
            <div class="mb-4">
                <label class="form-label">Descrição Geral</label>
                <textarea name="descricao" class="form-control" rows="5" placeholder="História e gameplay..."><%= jogo ? jogo.descricao : '' %></textarea>
            </div>
            <div class="mb-4">
                <label class="form-label">Requisitos Técnicos</label>
                <textarea name="requisitos" class="form-control" rows="3" placeholder="RAM, CPU, GPU..."><%= jogo ? jogo.requisitos : '' %></textarea>
            </div>

            <div class="d-flex align-items-center justify-content-between p-3 bg-light rounded-4">
                <h6 class="fw-bold m-0"><i class="fa-solid fa-eye me-2"></i> Visível na Loja</h6>
                <div class="form-switch">
                    <input class="form-check-input" type="checkbox" name="ativo" id="ativo" <%= (!jogo || jogo.ativo) ? 'checked' : '' %>>
                </div>
            </div>

            <button type="submit" class="btn-publish">
                <i class="fa-solid fa-paper-plane"></i> PUBLICAR JOGO NO CATÁLOGO
            </button>
        </div>

    </form>
</div>

<script>
    // Função para adicionar campos de download
    function addLink() {
        const container = document.getElementById('linksContainer');
        const div = document.createElement('div');
        div.className = 'link-item';
        div.innerHTML = `
            <input type="text" name="links_labels[]" class="form-control" placeholder="Servidor" required>
            <input type="url" name="links_urls[]" class="form-control" placeholder="URL" required>
            <button type="button" class="btn-del-link" onclick="this.parentElement.remove()"><i class="fa-solid fa-trash-can"></i></button>
        `;
        container.appendChild(div);
    }
</script>

</body>
</html>