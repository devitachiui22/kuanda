<!DOCTYPE html>
<html lang="pt-AO">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Minhas Compras | KuandaShop Premium</title>
    
    <!-- Fontes Premium -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    
    <!-- Frameworks -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* =========================================
           DESIGN SYSTEM: FIGMA-DRIVEN UI
           ========================================= */
        :root {
            --bg: #F8F9FD;
            --primary: #0F0F0F;
            --accent: #8B0000;
            --surface: #FFFFFF;
            --text-main: #1A1A1B;
            --text-dim: #64748B;
            --radius-lg: 24px;
            --radius-sm: 12px;
            --shadow-lux: 0 10px 30px -5px rgba(0,0,0,0.04), 0 20px 40px -10px rgba(0,0,0,0.06);
        }

        body {
            background-color: var(--bg);
            font-family: 'Raleway', sans-serif;
            color: var(--text-main);
            -webkit-font-smoothing: antialiased;
            padding-bottom: 50px;
        }

        /* REMOVER QUALQUER SUBLINHADO E NAVBAR PADRÃO */
        a { text-decoration: none !important; color: inherit; transition: 0.3s; }
        
        /* HEADER MODERNIZADO (SEM NAVBAR) */
        .header-wrapper {
            padding: 30px 0;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .brand-logo {
            font-weight: 900;
            font-size: 1.5rem;
            letter-spacing: -1px;
            color: var(--primary);
        }

        .brand-logo span { color: var(--accent); }

        .btn-profile {
            background: var(--white);
            border: 1px solid rgba(0,0,0,0.05);
            padding: 10px 18px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 0.85rem;
            box-shadow: 0 4px 10px rgba(0,0,0,0.03);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        /* TÍTULO DA PÁGINA */
        .page-header {
            margin-bottom: 35px;
        }

        .page-title {
            font-weight: 900;
            font-size: 2.2rem;
            letter-spacing: -1.2px;
            margin: 0;
        }

        /* STATUS BADGES MODERNOS */
        .status-pill {
            padding: 6px 14px;
            border-radius: 50px;
            font-size: 0.7rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-block;
        }

        .status-pendente { background: #FFF9E6; color: #D97706; }
        .status-confirmado { background: #E0F2FE; color: #0284C7; }
        .status-enviado { background: #F5F3FF; color: #7C3AED; }
        .status-entregue { background: #F0FDF4; color: #16A34A; }
        .status-cancelado { background: #FEF2F2; color: #DC2626; }

        /* CARDS DE PEDIDO */
        .order-card {
            background: var(--surface);
            border-radius: var(--radius-lg);
            border: 1px solid rgba(0,0,0,0.03);
            box-shadow: var(--shadow-lux);
            padding: 24px;
            height: 100%;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
        }

        .order-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 30px 60px -15px rgba(0,0,0,0.1);
        }

        .order-id {
            font-size: 0.75rem;
            font-weight: 800;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 12px;
            display: block;
        }

        .order-shop {
            font-weight: 800;
            font-size: 1.1rem;
            margin-bottom: 5px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .order-date {
            font-size: 0.85rem;
            color: var(--text-dim);
            font-weight: 500;
            margin-bottom: 20px;
        }

        .order-total {
            font-size: 1.4rem;
            font-weight: 900;
            color: var(--accent);
            margin: 15px 0;
        }

        .btn-details {
            width: 100%;
            background: #F8FAFC;
            border: 1px solid #E2E8F0;
            padding: 12px;
            border-radius: var(--radius-sm);
            font-weight: 700;
            font-size: 0.85rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            margin-top: 10px;
        }

        .btn-details:hover {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }

        /* ICONES 3D FLUENCY */
        .icon-3d {
            width: 28px;
            height: 28px;
            object-fit: contain;
        }

        /* ESTADO VAZIO */
        .empty-state {
            background: white;
            border-radius: var(--radius-lg);
            padding: 60px 20px;
            text-align: center;
            box-shadow: var(--shadow-lux);
        }

        .empty-icon { width: 120px; margin-bottom: 25px; }

        /* =========================================
           RESPONSIVIDADE: MINIATURIZAÇÃO 3x3
           ========================================= */
        @media (max-width: 600px) {
            .page-title { font-size: 1.6rem; }
            
            /* GRID 3x3 PARA PEDIDOS */
            .orders-grid {
                display: grid !important;
                grid-template-columns: repeat(3, 1fr) !important;
                gap: 10px !important;
            }

            .order-card {
                padding: 12px 8px !important;
                border-radius: 16px;
                text-align: center;
                display: flex;
                flex-direction: column;
                justify-content: space-between;
                min-height: 160px;
            }

            .order-id { font-size: 0.55rem; letter-spacing: 0; margin-bottom: 5px; }
            
            .order-shop { 
                font-size: 0.65rem !important; 
                flex-direction: column;
                gap: 2px;
                line-height: 1.2;
                height: 32px;
                overflow: hidden;
            }
            .order-shop img { width: 20px !important; height: 20px !important; }

            .order-date { font-size: 0.6rem; margin-bottom: 8px; }
            
            .status-pill {
                font-size: 0.5rem;
                padding: 3px 6px;
                white-space: nowrap;
            }

            .order-total { font-size: 0.75rem !important; margin: 8px 0 !important; }

            .btn-details {
                font-size: 0.6rem;
                padding: 6px;
                border-radius: 8px;
            }
            .btn-details i { display: none; }
        }
    </style>
</head>
<body>

    <div class="container">

        <!-- Cabeçalho de Conteúdo -->
        <div class="page-header d-flex justify-content-between align-items-end">
            <div>
                <h1 class="page-title">Minhas Compras</h1>
                <p class="text-muted fw-500 mb-0">Acompanhe o status dos seus pedidos em tempo real.</p>
            </div>
            <a href="/produtos" class="btn-profile d-none d-md-flex" style="background: var(--accent); color: white; border: none;">
                <img src="https://img.icons8.com/3d-fluency/94/shopping-cart.png" width="22" alt="">
                Comprar mais
            </a>
        </div>

        <% if (pedidos.length === 0) { %>
            <div class="empty-state">
                <img src="https://img.icons8.com/3d-fluency/188/empty-box.png" class="empty-icon" alt="Vazio">
                <h3 class="fw-800">Nada por aqui... ainda!</h3>
                <p class="text-muted mb-4">Você ainda não realizou compras. Que tal explorar nossas ofertas?</p>
                <a href="/produtos" class="btn btn-dark px-5 py-3 rounded-pill fw-800">
                    Começar a Comprar
                </a>
            </div>
        <% } else { %>
            <div class="row orders-grid g-4">
                <% pedidos.forEach(pedido => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="order-card">
                            <span class="order-id">#<%= pedido.codigo %></span>
                            
                            <div class="order-shop">
                                <img src="https://img.icons8.com/3d-fluency/94/shop.png" class="icon-3d" alt="">
                                <span><%= pedido.nome_loja %></span>
                            </div>

                            <div class="order-date">
                                <i class="far fa-calendar-alt me-1"></i> 
                                <%= new Date(pedido.created_at).toLocaleDateString('pt-AO') %>
                            </div>

                            <% 
                                let statusClass = 'status-pendente';
                                if(pedido.status === 'confirmado') statusClass = 'status-confirmado';
                                if(pedido.status === 'enviado') statusClass = 'status-enviado';
                                if(pedido.status === 'entregue') statusClass = 'status-entregue';
                                if(pedido.status === 'cancelado') statusClass = 'status-cancelado';
                            %>
                            <div class="mb-2">
                                <span class="status-pill <%= statusClass %>"><%= pedido.status %></span>
                            </div>

                            <div class="order-total">
                                <%= parseFloat(pedido.total).toLocaleString() %> <small style="font-size: 0.8rem;">Kz</small>
                            </div>

                            <a href="/meu-pedido/<%= pedido.id %>" class="btn-details">
                                Detalhes <i class="fas fa-chevron-right" style="font-size: 0.7rem;"></i>
                            </a>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>

    <!-- Scripts Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>