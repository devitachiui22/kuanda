<!DOCTYPE html>
<html lang="pt-AO">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>Gestão de Vendas | Painel do Vendedor KuandaShop</title>
    
    <!-- CSS & Fonts -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        /* =========================================
           KUANDA GLOBAL DESIGN SYSTEM
           ========================================= */
        :root {
            --k-red: #E31C25;
            --k-red-dark: #B01018;
            --k-black: #0F0F0F;
            --k-gray: #F3F4F6;
            --radius-md: 16px;
            --radius-lg: 24px;
            --shadow-lux: 0 10px 30px -5px rgba(0, 0, 0, 0.05);
            --glass-bg: rgba(255, 255, 255, 0.85);
            --glass-blur: blur(12px);
            --kf-bg-dark: #09090b;
            --kf-text: #e4e4e7;
        }

        body {
            font-family: 'Plus Jakarta Sans', sans-serif;
            background-color: #FAFAFA;
            color: var(--k-black);
            padding-top: 100px;
            margin: 0;
            -webkit-font-smoothing: antialiased;
        }

        a { text-decoration: none !important; color: inherit; transition: 0.2s; }

        /* NAVBAR CÁPSULA VENDEDOR */
        .nav-capsule {
            position: fixed; top: 20px; left: 50%; transform: translateX(-50%);
            width: 90%; max-width: 1200px; background: var(--glass-bg);
            backdrop-filter: var(--glass-blur); -webkit-backdrop-filter: var(--glass-blur);
            border: 1px solid rgba(255,255,255,0.5); border-radius: 100px;
            padding: 10px 25px; z-index: 1000; box-shadow: 0 10px 30px rgba(0,0,0,0.05);
            display: flex; align-items: center; justify-content: space-between;
        }

        /* KPI CARDS */
        .kpi-card-lux {
            background: white; border-radius: var(--radius-lg); padding: 25px;
            border: 1px solid rgba(0,0,0,0.03); box-shadow: var(--shadow-lux);
            height: 100%; transition: transform 0.3s ease;
            display: flex; flex-direction: column; justify-content: space-between;
        }
        .kpi-card-lux:hover { transform: translateY(-5px); }
        .kpi-label { font-size: 0.75rem; font-weight: 700; color: #64748B; text-transform: uppercase; letter-spacing: 1px; }
        .kpi-value { font-size: 1.6rem; font-weight: 800; margin-top: 8px; color: var(--k-black); }
        .kpi-icon-box {
            width: 48px; height: 48px; border-radius: 14px;
            display: flex; align-items: center; justify-content: center;
        }

        /* PAINEL DE TABELA */
        .glass-panel {
            background: white; border-radius: var(--radius-lg); padding: 30px;
            border: 1px solid rgba(0,0,0,0.03); box-shadow: var(--shadow-lux); margin-bottom: 30px;
        }
        .panel-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 25px; }
        .panel-title { font-weight: 800; font-size: 1.25rem; letter-spacing: -0.5px; margin: 0; }

        /* STATUS BADGES */
        .st-badge { padding: 6px 12px; border-radius: 50px; font-size: 0.65rem; font-weight: 800; text-transform: uppercase; letter-spacing: 0.5px; }
        .st-pendente { background: #FEF3C7; color: #92400E; }
        .st-confirmado { background: #E0F2FE; color: #0369A1; }
        .st-enviado { background: #F5F3FF; color: #6D28D9; }
        .st-entregue { background: #DCFCE7; color: #166534; }
        .st-cancelado { background: #FEE2E2; color: #991B1B; }

        /* TABELA CUSTOM */
        .table-lux { width: 100%; vertical-align: middle; }
        .table-lux thead th { background: #F8FAFC; padding: 15px; font-size: 0.7rem; font-weight: 800; color: #64748B; text-transform: uppercase; border: none; }
        .table-lux tbody td { padding: 18px 15px; border-bottom: 1px solid #F1F5F9; font-size: 0.85rem; font-weight: 600; }

        /* =========================================
           RESPONSIVIDADE: MATRIX 3x3 MINI
           ========================================= */
        @media (max-width: 576px) {
            .nav-capsule { padding: 8px 15px; width: 95%; }
            .table-responsive { display: none; } /* Esconde tabela no mobile */

            .matrix-3x3-admin {
                display: grid !important;
                grid-template-columns: repeat(3, 1fr) !important;
                gap: 10px;
            }

            .mini-order-tile {
                background: #F8FAFC; padding: 12px 5px; border-radius: 18px;
                text-align: center; border: 1px solid #E2E8F0;
                display: flex; flex-direction: column; align-items: center; justify-content: center;
                min-height: 120px;
            }
            .mini-id { font-size: 0.6rem; font-weight: 800; color: var(--k-red); margin-bottom: 4px; }
            .mini-client { font-size: 0.65rem; font-weight: 700; height: 32px; overflow: hidden; line-height: 1.2; }
            .mini-total { font-size: 0.75rem; font-weight: 800; margin-top: 5px; color: var(--k-black); }
            .mini-status-dot { width: 6px; height: 6px; border-radius: 50%; margin-top: 6px; }
            
            .btn-manage-mini { font-size: 0.6rem; font-weight: 800; margin-top: 8px; color: var(--k-red); }
        }

        /* GAMER FOOTER */
        .gamer-footer { background: var(--kf-bg-dark); color: var(--kf-text); padding: 60px 0 100px; margin-top: 50px; }
        .gf-title { font-weight: 800; font-size: 1.5rem; text-transform: uppercase; background: linear-gradient(90deg, #fff, #666); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .gf-scroll-wrapper { display: flex; gap: 20px; overflow-x: auto; padding: 20px; scrollbar-width: none; }
        .game-card { min-width: 140px; height: 200px; border-radius: 12px; position: relative; overflow: hidden; border: 1px solid #27272a; }
        .game-img { width: 100%; height: 100%; object-fit: cover; opacity: 0.7; }
        .game-overlay { position: absolute; bottom: 0; padding: 10px; background: linear-gradient(to top, #000, transparent); width: 100%; color: white; font-size: 0.75rem; font-weight: 700; }
    </style>
</head>
<body>

    <!-- NAVBAR CAPSULE -->
    <nav class="nav-capsule">
        <a href="/vendedor" class="d-flex align-items-center gap-2">
            <img src="https://img.icons8.com/3d-fluency/94/shop.png" width="30">
            <span class="fw-bold d-none d-sm-inline">Vendedor<span class="text-danger">.</span></span>
        </a>
        <div class="d-flex gap-2">
            <a href="/vendedor/produto/novo" class="btn btn-dark btn-sm rounded-pill px-3 fw-bold"><i class="fas fa-plus me-1"></i> Novo</a>
            <a href="/perfil" class="btn btn-outline-dark btn-sm rounded-pill px-3 fw-bold">Perfil</a>
        </div>
    </nav>

    <div class="container py-4">
        <header class="mb-5">
            <h1 class="fw-900 fs-2" style="letter-spacing: -1px;">Gestão de Vendas</h1>
            <p class="text-muted fw-600">Monitore seus pedidos e faturamento em tempo real.</p>
        </header>

        <!-- ESTATÍSTICAS (KPIs) -->
        <div class="row g-4 mb-5">
            <div class="col-md-4">
                <div class="kpi-card-lux">
                    <span class="kpi-label">Faturamento Entregue</span>
                    <div class="d-flex justify-content-between align-items-end">
                        <div class="kpi-value"><%= parseFloat(stats.receita_total).toLocaleString() %> <small style="font-size: 0.8rem;">Kz</small></div>
                        <div class="kpi-icon-box" style="background: #DCFCE7; color: #166534;"><i class="fas fa-wallet"></i></div>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="kpi-card-lux">
                    <span class="kpi-label">Aguardando Preparo</span>
                    <div class="d-flex justify-content-between align-items-end">
                        <div class="kpi-value"><%= stats.pendentes %> <small style="font-size: 0.8rem;">Pedidos</small></div>
                        <div class="kpi-icon-box" style="background: #FEF3C7; color: #92400E;"><i class="fas fa-clock"></i></div>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="kpi-card-lux">
                    <span class="kpi-label">Total de Vendas</span>
                    <div class="d-flex justify-content-between align-items-end">
                        <div class="kpi-value"><%= stats.total_pedidos %> <small style="font-size: 0.8rem;">Vendas</small></div>
                        <div class="kpi-icon-box" style="background: #E0F2FE; color: #0369A1;"><i class="fas fa-shopping-bag"></i></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- LISTA DE PEDIDOS -->
        <div class="glass-panel">
            <div class="panel-header">
                <h2 class="panel-title"><i class="fas fa-list-ul me-2 text-danger"></i>Histórico Recente</h2>
            </div>

            <!-- Desktop View -->
            <div class="table-responsive">
                <table class="table table-lux">
                    <thead>
                        <tr>
                            <th>Ref</th>
                            <th>Data</th>
                            <th>Cliente</th>
                            <th>Valor Total</th>
                            <th>Status</th>
                            <th class="text-end">Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (pedidos.length === 0) { %>
                            <tr>
                                <td colspan="6" class="text-center py-5">
                                    <img src="https://img.icons8.com/3d-fluency/188/empty-box.png" width="80" class="mb-3">
                                    <p class="text-muted fw-bold">Nenhum pedido recebido até o momento.</p>
                                </td>
                            </tr>
                        <% } else { %>
                            <% pedidos.forEach(p => { %>
                                <tr>
                                    <td class="fw-800 text-danger">#<%= p.codigo || p.id %></td>
                                    <td class="text-muted small"><%= new Date(p.created_at).toLocaleDateString('pt-AO') %></td>
                                    <td>
                                        <div class="d-flex align-items-center gap-2">
                                            <div class="rounded-circle bg-light d-flex justify-content-center align-items-center" style="width: 32px; height: 32px;">
                                                <i class="fas fa-user-circle text-secondary"></i>
                                            </div>
                                            <span class="fw-700"><%= p.cliente_nome %></span>
                                        </div>
                                    </td>
                                    <td class="fw-800"><%= parseFloat(p.total).toLocaleString() %> Kz</td>
                                    <td>
                                        <% 
                                            let st = 'st-pendente';
                                            if(p.status === 'confirmado') st = 'st-confirmado';
                                            if(p.status === 'enviado') st = 'st-enviado';
                                            if(p.status === 'entregue') st = 'st-entregue';
                                            if(p.status === 'cancelado') st = 'st-cancelado';
                                        %>
                                        <span class="st-badge <%= st %>"><%= p.status %></span>
                                    </td>
                                    <td class="text-end">
                                        <a href="/vendedor/pedido/<%= p.id %>" class="btn btn-sm btn-dark rounded-pill px-3 fw-800">
                                            Gerenciar
                                        </a>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>
                </table>
            </div>

            <!-- Mobile View: Matrix 3x3 -->
            <div class="matrix-3x3-admin d-sm-none">
                <% pedidos.forEach(p => { %>
                    <div class="mini-order-tile">
                        <span class="mini-id">#<%= p.codigo || p.id %></span>
                        <span class="mini-client"><%= p.cliente_nome %></span>
                        <span class="mini-total"><%= parseFloat(p.total).toLocaleString() %> Kz</span>
                        <% 
                            let dotColor = '#EAB308'; // pendente
                            if(p.status === 'entregue') dotColor = '#16A34A';
                            if(p.status === 'cancelado') dotColor = '#DC2626';
                            if(p.status === 'enviado') dotColor = '#7C3AED';
                        %>
                        <div class="mini-status-dot" style="background: <%= dotColor %>;"></div>
                        <a href="/vendedor/pedido/<%= p.id %>" class="btn-manage-mini">Gerir <i class="fas fa-arrow-right"></i></a>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>